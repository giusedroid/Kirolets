[build-system]
requires = ["setuptools>=69"]
build-backend = "setuptools.build_meta"

[project]
name = "kirolets-bot"
version = "0.1.0"
description = "Telegram bot for Kirolets"
readme = "README.md"
requires-python = ">=3.14"
dependencies = [
    "boto3>=1.35.0",
    "python-dotenv>=1.0.1",
    "python-telegram-bot>=21.0,<22.0",
    "redis>=5.2.0",
]

[dependency-groups]
dev = [
    "pytest>=8.0",
    "ruff>=0.5.0",
]

[project.scripts]
kirolets-bot = "kirolets_bot.main:main"
kirolets-worker = "kirolets_bot.worker:main"

[tool.setuptools.packages.find]
where = ["src"]

[tool.pytest.ini_options]
testpaths = ["tests"]

[tool.ruff]
line-length = 100
target-version = "py314"
